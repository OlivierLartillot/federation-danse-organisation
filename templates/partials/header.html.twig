    <!--==============================
	Header Area
    ==============================-->
    <header class="th-header header-layout1">
        <div class="header-top">
            <div class="container">
                <div class="row justify-content-center justify-content-lg-between align-items-center gy-2">
                    <div class="col-auto d-none d-lg-block">
                        <div class="header-links">
                            <ul>
                                <li><i class="fa-solid fa-phone"></i><a href="tel:+1631202-0088">+(163)-1202-0088</a></li>
                                <li><i class="fa-solid fa-envelope"></i><a href="mailto:info@Danza.com">help24/7@gmail.com</a></li>
                                <li><i class="fa-solid fa-location-dot"></i>835 Middle Country Rd, NY 11784, USA</li>
                            </ul>
                        </div>
                    </div>
                    <div class="col-auto">
                        <div class="header-social">
                            <span class="social-title">Follow Us : </span>
                            <a href="https://www.facebook.com/"><i class="fab fa-facebook-f"></i></a>
                            <a href="https://www.twitter.com/"><i class="fab fa-twitter"></i></a>
                            <a href="https://www.linkedin.com/"><i class="fab fa-linkedin-in"></i></a>
                            <a href="https://www.google.com/"><i class="fa-brands fa-google"></i></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="sticky-wrapper">
            <!-- Main Menu Area -->
            <div class="menu-area">
                <div class="container">
                    <div class="row align-items-center justify-content-between">
                        <div class="col-auto">
                            <div class="header-logo">
                                <a href="{{path('app_main')}}" class="fs-5"><img src="{{ asset('assets/img/logo.svg')}}" alt="Logo FDO" style="max-height:60px; max-width: 200px"></a>
                            </div>
                        </div>
                        <div class="col-auto">
                            <nav class="main-menu d-none d-lg-inline-block">
                                <ul>
                                    <li><a href="{{path('app_main')}}">Accueil</a></li>
                                    <li><a href="{{path('app_clubs')}}">Clubs</a></li>
                                    <li class="menu-item-has-children">
                                        <a href="#">Compétition</a>
                                        <ul class="sub-menu">
                                            <li>
                                                <a href="{{path('app_competition_reglement')}}">Règlement</a>
                                            </li>
                                            <li>
                                                <a href="#">Résultats</a>
                                            </li>
                                            <li>
                                                <a href="{{path('app_competition_dossards')}}">Dossards</a>
                                            </li>
                                        </ul>
                                    </li>
                                    <li>
                                        <a href="contact.html">Contact</a>
                                    </li>

                                    <!-- MENU CONNECTE-->
                                    {% if is_granted('ROLE_CLUB') %}
                                    <li class="menu-item-has-children">
                                        <a href="#">
                                            {% set displayErrors = false %} 

                                            {% if (('ROLE_CLUB' in app.user.roles) and (app.session.get('licencesRejetees'))) > 0 %}
                                                {% set displayErrors = true %} 
                                            {% endif %}
                                            {% if ((is_granted('ROLE_SECRETAIRE')) and (app.session.get('licencesEnCours') > 0)) %}
                                                {% set displayErrors = true %} 
                                            {% endif %}                                            

                                            {% if displayErrors %}     
                                                <div class="position-relative d-inline p-1">
                                                    Gestion
                                                    <span class="position-absolute top-0 start-0 translate-middle p-2 bg-danger border border-light rounded-circle">
                                                        {# <span class="visually-hidden">unread messages</span> #}
                                                    </span>
                                                </div>
                                            {% else %}
                                                Gestion
                                            {% endif %}



                                        </a>
                                        <ul class="sub-menu">
                                            {% if is_granted('ROLE_SUPERMAN') %}
                                            <li class="menu-item-has-children">
                                                <a href="#">Réglages du site</a>
                                                <ul class="sub-menu">
                                                    <li><a href="{{path('app_admin_user_index')}}">Utilisateurs</a></li>
                                                    <li>
                                                        <a href="{{path('app_settings_edit', {id:1})}}">Paramètres du site</a>
                                                    </li>       
                                                    <li><a href="{{path('app_admin_organization_team_index')}}">Dirigeants FDO</a></li>                                                                
                                                </ul>
                                            </li>
                                            {% endif %}
                                            {% if is_granted('ROLE_DIRECTEUR_TECHNIQUE') %}
                                            <li class="menu-item-has-children">
                                                <a href="#">Réglages Compet</a>
                                                <ul class="sub-menu">
                                                    <li><a href="{{path('app_admin_season_index')}}">Saisons</a></li>
                                                    <li><a href="{{path('app_admin_championship_index')}}">Championnats</a></li>                                                    
                                                    <li><a href="{{path('app_admin_category_index')}}">Catégories</a></li>
                                                    <li><a href="{{path('app_admin_document_index')}}">Documents et règlements</a></li>
                                                </ul>
                                            </li>
                                            {% endif %}
                                            <li class="menu-item-has-children">
                                                <a href="#">Clubs</a>
                                                <ul class="sub-menu">
                                                <!-- Modifier la liste des clubs ou mon club n'appartient qu'au club ou au directeur et président-->
                                                    {% if is_granted('ROLE_DIRECTEUR_TECHNIQUE') or ('ROLE_CLUB' in app.user.roles) %}
                                                    <li>
                                                        <a href="{{path('app_admin_club_index')}}">Clubs</a>
                                                    </li>
                                                    {% endif %}
                                                    <li><a href="{{path('app_admin_danseur_index')}}">Danseurs</a></li>
                                                    <li>
                                                        <a href="{{path('app_admin_licence_index')}}">
                                                            <div class="position-relative">
                                                                Licences
                                                                <!-- Si tu es un role club affiche les licences rejetées --> 
                                                                {% if (('ROLE_CLUB' in app.user.roles) and (app.session.get('licencesRejetees'))) > 0 %}
                                                                    <span class="position-absolute top-50 start-10 translate-middle badge rounded-pill bg-danger">
                                                                        {{ app.session.get('licencesRejetees') }}      
                                                                        {# <span class="visually-hidden">unread messages</span> #}
                                                                    </span>
                                                                {% endif %}

                                                                <!-- Si tu es un au moins un secrétaire les licences en cours --> 
                                                                {% if ((is_granted('ROLE_SECRETAIRE')) and (app.session.get('licencesEnCours') > 0)) %}
                                                                    <span class="position-absolute top-50 start-10 translate-middle badge rounded-pill bg-danger">
                                                                        {{ app.session.get('licencesEnCours') }}      
                                                                        {# <span class="visually-hidden">unread messages</span> #}
                                                                    </span>
                                                                {% endif %}
                                                            </div> 
                                                        </a>
                                                    </li>
                                                    <li><a href="{{path('app_competition_dossards')}}">Dossards</a></li>
                                                    <li><a href="{{path('app_admin_championship_index_inscriptions')}}">Inscription Championnats</a></li>
                                                </ul>
                                            </li>
                                            <li><a href="{{path('app_admin_user_index')}}">Mon Compte</a></li>
                                        </ul>
                                    </li>
                                    {% endif %}



                                </ul>
                            </nav>
                            <button type="button" class="th-menu-toggle d-inline-block d-lg-none"><i class="far fa-bars"></i></button>
                        </div>
                        <div class="col-auto d-none d-xl-block">
                            <div class="header-button">
                                {#                                 
                                <button type="button" class="icon-btn searchBoxToggler"><i class="fal fa-search"></i></button>
                                <button type="button" class="icon-btn sideMenuToggler2">
                                    <i class="fal fa-shopping-cart"></i>
                                    <span class="badge">5</span>
                                </button>
                                 #}
                                <button type="button" class="icon-btn fs-6 sideMenuToggler">
                                    <i class="fa-thin fa-grid"></i>
                                </button>
                                {% if app.user %}
                                    <a href="{{ path('app_logout')}}" class="th-btn ">Déconnexion</a>
                                {% else %}
                                    <a href="{{ path('app_login')}}" class="th-btn th-btn--connexion">Connexion</a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="logo-bg"></div>
            <div class="menu-bg"></div>
        </div>
    </header>