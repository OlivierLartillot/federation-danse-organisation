<form method="post" action="{{ path('app_admin_inscription_championnat_delete', {'id': inscription_championnat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription_championnat.id) }}">
    <button class="btn">Delete</button>
</form>
