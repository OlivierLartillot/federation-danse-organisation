{% extends 'base.html.twig' %}

{% block title %}Licence{% endblock %}

{% block body %}
    <h1>Licence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ licence.id }}</td>
            </tr>
            <tr>
                <th>Dossard</th>
                <td>{{ licence.dossard }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ licence.status }}</td>
            </tr>
            <tr>
                <th>Archived</th>
                <td>{{ licence.archived ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_licence_index') }}">back to list</a>

    <a href="{{ path('app_licence_edit', {'id': licence.id}) }}">edit</a>

    {{ include('licence/_delete_form.html.twig') }}
{% endblock %}
