{{ form_start(form) }}


    {{ form_row(form.name) }}
    {{ form_row(form.organizationRole) }}
    {{ form_row(form.imageFile) }}



    <button class="btn btn-success my-5">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
