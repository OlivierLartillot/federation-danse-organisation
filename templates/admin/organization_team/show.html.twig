{% extends 'base.html.twig' %}

{% block title %}OrganizationTeam{% endblock %}

{% block body %}
    <h1>OrganizationTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organization_team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organization_team.name }}</td>
            </tr>
            <tr>
                <th>OrganizationRole</th>
                <td>{{ organization_team.organizationRole }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_organization_team_index') }}">back to list</a>

    <a href="{{ path('app_admin_organization_team_edit', {'id': organization_team.id}) }}">edit</a>

    {{ include('admin/organization_team/_delete_form.html.twig') }}
{% endblock %}
