{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ season.name }}</td>
            </tr>
            <tr>
                <th>IsCurrentSeason</th>
                <td>{{ season.isCurrentSeason ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_season_index') }}">back to list</a>

    <a href="{{ path('app_admin_season_edit', {'id': season.id}) }}">edit</a>

    {{ include('admin/season/_delete_form.html.twig') }}
{% endblock %}
