<form method="post" action="{{ path('app_admin_licence_comment_delete', {'id': licence_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ licence_comment.id) }}">
    <button class="btn">Delete</button>
</form>
