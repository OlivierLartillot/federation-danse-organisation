{% extends 'base.html.twig' %}

{% block title %}LicenceComment{% endblock %}

{% block body %}
    <h1>LicenceComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ licence_comment.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_licence_comment_index') }}">back to list</a>

    <a href="{{ path('app_admin_licence_comment_edit', {'id': licence_comment.id}) }}">edit</a>

    {{ include('admin/licence_comment/_delete_form.html.twig') }}
{% endblock %}
